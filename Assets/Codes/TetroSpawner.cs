using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TetroSpawner : MonoBehaviour
{
    public GameObject[] tetro;
    // Start is called before the first frame update
    void Start()
    {
        /*
        te.GetComponent<TetroBehav>().parentBoard = transform.gameObject;
        Instantiate(te);
        te.transform.position = transform.position;
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
